var assert = require("assert");
describe("un test juste comme ça", function () {
   it("should assert true", function () {
        assert.equal(true, true);
    });
    it("should assert true2", function () {
        assert.equal(true, true);
    });
});